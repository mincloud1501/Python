"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getPropTypes = getPropTypes;
exports.isFunctionProp = isFunctionProp;

var _typeof2 = _interopRequireDefault(require("@babel/runtime/helpers/typeof"));

function getPropTypes(Class) {
  var propTypes = Class && Class._propTypes && Class._propTypes;

  if (!propTypes && Class && Class.defaultProps) {
    new Class({});
    propTypes = Class && Class._propTypes && Class._propTypes;
  }

  return propTypes;
}

function isFunctionProp(propTypes, propName) {
  var propType = propTypes && propTypes[propName];

  if (!propType) {
    return propName.startsWith('get');
  }

  var type = (0, _typeof2["default"])(propType) === 'object' && propType.type;

  switch (type) {
    case 'accessor':
    case 'function':
      return true;

    default:
      return false;
  }
}
//# sourceMappingURL=deck-prop-types.js.map